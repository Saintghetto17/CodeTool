# 🎯 Инструкция по демонстрации работы Code Agent

## 📋 Что нужно продемонстрировать для конкурса

Согласно требованиям, участник должен показать:
- ✅ Примеры Issues и Pull Requests, демонстрирующие работу системы
- ✅ Автоматизацию полного SDLC цикла
- ✅ Code Agent: анализ Issue → изменение кода → создание PR
- ✅ AI Reviewer: анализ PR → проверка CI → публикация review

---

## 🚀 Способ 1: Локальная демонстрация (быстро, без GitHub API)

### Подготовка

```bash
cd /home/ilyanovitskiy/code_agent

# Установите зависимости (если еще не установлены)
pip install -r requirements.txt

# Создайте .env файл для тестирования
cat > .env << 'EOF'
GITHUB_TOKEN=demo_token_not_used_in_local_mode
GITHUB_REPO=demo/repo
OPENAI_API_KEY=demo_key_not_used
LLM_PROVIDER=openai
LOG_LEVEL=INFO
EOF
```

### Тест 1: Проверка CLI

```bash
# Проверьте версию
python -m code_agent.cli version

# Должно вывести:
# Code Agent version: 1.0.0
```

### Тест 2: Проверка через Docker

```bash
# Соберите образ (уже собран)
docker compose build

# Запустите команду version
docker compose run --rm code-agent version

# Должно вывести:
# Code Agent version: 1.0.0
```

### Тест 3: Проверка модулей

```bash
# Запустите тесты
pytest tests/unit/ -v

# Должно показать:
# ✅ 17 passed
```

---

## 🌐 Способ 2: Демонстрация с реальным GitHub (полная интеграция)

### Шаг 1: Подготовка GitHub репозитория

1. **Создайте тестовый репозиторий:**
   ```
   Имя: code-agent-demo
   Visibility: Public
   Инициализируйте с README
   ```

2. **Получите GitHub Personal Access Token:**
   - Перейдите: https://github.com/settings/tokens
   - Generate new token (classic)
   - Выберите scopes:
     - ✅ `repo` (full control)
     - ✅ `workflow`
   - Скопируйте токен

3. **Настройте .env файл:**
   ```bash
   cd /home/ilyanovitskiy/code_agent
   
   cat > .env << 'EOF'
   GITHUB_TOKEN=ghp_ваш_токен_здесь
   GITHUB_REPO=ваш_username/code-agent-demo
   OPENAI_API_KEY=sk-ваш_ключ_openai
   LLM_PROVIDER=openai
   MAX_ITERATIONS=3
   LOG_LEVEL=INFO
   EOF
   ```

### Шаг 2: Создайте тестовый Issue в GitHub

Откройте ваш репозиторий и создайте новый Issue:

**Пример Issue #1:**
```
Title: Добавить функцию для вычисления суммы двух чисел

Description:
Создайте файл `calculator.py` с функцией `add(a, b)`, которая:
- Принимает два числа
- Возвращает их сумму
- Имеет docstring с описанием

Также добавьте простой тест для этой функции.
```

### Шаг 3: Запустите Code Agent

```bash
cd /home/ilyanovitskiy/code_agent

# Обработайте Issue #1
python -m code_agent.cli process-issue 1

# Или через Docker:
docker compose run --rm code-agent process-issue 1
```

**Что произойдет:**
1. ✅ Agent прочитает Issue #1
2. ✅ Проанализирует требования через LLM
3. ✅ Создаст ветку `agent/issue-1`
4. ✅ Сгенерирует код `calculator.py`
5. ✅ Создаст тест `test_calculator.py`
6. ✅ Закоммитит изменения
7. ✅ Создаст Pull Request
8. ✅ Выведет ссылку на PR

### Шаг 4: Проверьте результат

1. **Откройте созданный Pull Request в GitHub**
2. **Проверьте:**
   - ✅ Описание PR ссылается на Issue #1
   - ✅ Код добавлен в правильные файлы
   - ✅ Коммит содержит осмысленное сообщение
   - ✅ CI запустился (если настроен)

### Шаг 5: Запустите AI Reviewer

```bash
# Проанализируйте созданный PR (например, #2)
python -m code_agent.cli review-pr 2

# Или через Docker:
docker compose run --rm code-agent review-pr 2
```

**Что произойдет:**
1. ✅ Reviewer прочитает PR и diff
2. ✅ Проанализирует код через LLM
3. ✅ Проверит соответствие Issue требованиям
4. ✅ Проверит результаты CI (если запущены)
5. ✅ Опубликует review комментарий в PR

---

## 📸 Что показать жюри конкурса

### 1. Скриншоты GitHub

**Issue:**
- 📸 Скриншот созданного Issue с требованиями

**Pull Request:**
- 📸 Скриншот PR с кодом
- 📸 Скриншот файлов изменений (Files changed)
- 📸 Скриншот автоматического коммита от бота

**Review:**
- 📸 Скриншот AI review комментария
- 📸 Скриншот анализа CI результатов

### 2. Логи выполнения

```bash
# Сохраните логи в файл
python -m code_agent.cli process-issue 1 2>&1 | tee agent_logs.txt

# Покажите что логи содержат:
# - Анализ Issue
# - Генерацию кода через LLM
# - Создание коммитов
# - Создание PR
```

### 3. Видео демонстрация (опционально)

Запишите короткое видео (2-3 минуты):
1. Создание Issue
2. Запуск команды `process-issue`
3. Просмотр созданного PR
4. Запуск `review-pr`
5. Просмотр review комментария

---

## 🎬 Пример полного сценария демонстрации

### Сценарий "От Issue до Merge"

**Таймлайн:**

```
Минута 0: Создание Issue
  → Issue #1: "Добавить функцию валидации email"

Минута 1: Запуск Code Agent
  $ python -m code_agent.cli process-issue 1
  → Agent создает PR #2 с реализацией

Минута 3: Запуск Reviewer
  $ python -m code_agent.cli review-pr 2
  → Reviewer публикует review

Минута 4: Демонстрация результата
  → Показать PR с кодом и review
  → Показать что код соответствует Issue
  → (Опционально) Merge PR
```

---

## 🧪 Быстрая проверка без реальных API (Mock режим)

Если у вас нет API ключей, можете запустить тесты:

```bash
# Запустите unit тесты (они используют моки)
pytest tests/unit/ -v -s

# Вывод покажет:
# - Инициализацию агентов
# - Работу с моками LLM
# - Работу с моками GitHub API
# ✅ 17 passed
```

---

## 📊 Чек-лист для демонстрации

Перед демонстрацией убедитесь:

- [ ] ✅ Docker образ собран: `docker compose build`
- [ ] ✅ Тесты проходят: `pytest tests/unit/`
- [ ] ✅ Ruff проходит: `ruff check .`
- [ ] ✅ CLI работает: `python -m code_agent.cli version`
- [ ] ✅ .env файл настроен (для реального GitHub)
- [ ] ✅ GitHub token имеет нужные права
- [ ] ✅ Тестовый репозиторий создан
- [ ] ✅ Подготовлен пример Issue

---

## 🎯 Ключевые моменты для жюри

**Покажите что система:**

1. ✅ **Полностью автоматическая** - от Issue до PR без вмешательства человека
2. ✅ **Использует AI** - LLM для генерации кода и review
3. ✅ **Интегрирована с GitHub** - API, Issues, PRs, Comments
4. ✅ **Проверяет качество** - AI Reviewer анализирует код
5. ✅ **Итеративная** - может исправлять ошибки (MAX_ITERATIONS)
6. ✅ **Production-ready** - Docker, CI/CD, тесты, линтинг

---

## 🔧 Troubleshooting

### Ошибка: "GitHub token invalid"
```bash
# Проверьте токен:
curl -H "Authorization: token YOUR_TOKEN" https://api.github.com/user
```

### Ошибка: "OpenAI API key invalid"
```bash
# Проверьте ключ или используйте mock режим (тесты)
pytest tests/unit/test_llm.py -v
```

### Ошибка: "Repository not found"
```bash
# Проверьте формат GITHUB_REPO в .env:
# Должно быть: username/repository
# Не должно быть: https://github.com/username/repository
```

---

## 📞 Контакты для вопросов

Если возникнут проблемы при демонстрации:
- Проверьте логи: `tail -f logs/agent.log`
- Запустите в debug режиме: `LOG_LEVEL=DEBUG python -m code_agent.cli ...`
- Используйте тесты для проверки модулей: `pytest -v`

---

## 🏆 Готово!

Система полностью работает и готова к демонстрации. Выберите способ:
- **Быстро**: Локальные тесты (5 минут)
- **Полноценно**: Реальный GitHub (15-20 минут)

**Удачи на конкурсе! 🚀**

