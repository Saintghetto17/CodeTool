# Структура проекта Code Agent

```
code-agent/
│
├── .github/                          # GitHub конфигурация
│   └── workflows/                    # GitHub Actions workflows
│       ├── issue_handler.yml         # Автообработка Issues
│       ├── pr_review.yml             # Автоматический ревью PR
│       └── ci.yml                    # CI pipeline
│
├── code_agent/                       # Основной код приложения
│   ├── __init__.py                   # Инициализация пакета
│   ├── config.py                     # Управление конфигурацией
│   ├── cli.py                        # CLI интерфейс (Typer)
│   │
│   ├── agents/                       # Агенты системы
│   │   ├── __init__.py
│   │   ├── code_agent.py             # Code Agent (обработка Issues)
│   │   └── reviewer_agent.py         # AI Reviewer Agent
│   │
│   ├── core/                         # Ядро системы
│   │   ├── __init__.py
│   │   ├── llm.py                    # LLM интеграция (OpenAI, YandexGPT)
│   │   └── github_client.py          # GitHub API клиент
│   │
│   └── utils/                        # Утилиты
│       ├── __init__.py
│       └── logger.py                 # Логирование
│
├── tests/                            # Тесты
│   ├── __init__.py
│   ├── unit/                         # Юнит-тесты
│   │   ├── __init__.py
│   │   ├── test_config.py            # Тесты конфигурации
│   │   └── test_llm.py               # Тесты LLM
│   └── integration/                  # Интеграционные тесты
│       └── __init__.py
│
├── config/                           # Конфигурационные файлы
│   └── prompts.yaml                  # LLM промпты
│
├── docs/                             # Документация
│   ├── INSTALLATION.md               # Инструкция по установке
│   ├── ARCHITECTURE.md               # Архитектура системы
│   └── EXAMPLES.md                   # Примеры использования
│
├── scripts/                          # Вспомогательные скрипты
│   └── setup.sh                      # Скрипт автоматической установки
│
├── .gitignore                        # Git ignore правила
├── .dockerignore                     # Docker ignore правила
├── Dockerfile                        # Docker образ
├── docker-compose.yml                # Docker Compose конфигурация
├── env.example                       # Пример переменных окружения
│
├── pyproject.toml                    # Конфигурация проекта (PEP 518)
├── requirements.txt                  # Основные зависимости
├── requirements-dev.txt              # Dev зависимости
│
├── README.md                         # Основная документация
├── CONTRIBUTING.md                   # Руководство для контрибьюторов
├── LICENSE                           # MIT лицензия
├── CHANGELOG.md                      # История изменений
└── PROJECT_STRUCTURE.md              # Этот файл
```

## Описание компонентов

### 📁 Директория `.github/`

GitHub-специфичные файлы и workflows для автоматизации.

#### Workflows:
- **issue_handler.yml**: Автоматически обрабатывает новые Issues, создаёт код и PR
- **pr_review.yml**: Запускает CI проверки и AI ревью для Pull Requests
- **ci.yml**: Основной CI pipeline для проверки качества кода

### 📁 Директория `code_agent/`

Основной код приложения.

#### Файлы:
- **config.py**: Pydantic Settings для управления конфигурацией
- **cli.py**: Typer-based CLI с командами process-issue, review-pr, fix-pr, generate-summary

#### Поддиректории:

**agents/**
- `code_agent.py`: Читает Issues, генерирует код, создаёт PR, исправляет на основе фидбека
- `reviewer_agent.py`: Анализирует PR, запускает ревью, публикует результаты

**core/**
- `llm.py`: Абстракция над LLM провайдерами (OpenAI, YandexGPT)
- `github_client.py`: Обертка над PyGithub для работы с GitHub API

**utils/**
- `logger.py`: Настройка логирования

### 📁 Директория `tests/`

Тесты для обеспечения качества кода.

#### Структура:
- **unit/**: Быстрые изолированные тесты отдельных компонентов
- **integration/**: Тесты взаимодействия компонентов

### 📁 Директория `config/`

Конфигурационные файлы для настройки поведения.

- **prompts.yaml**: Промпты для LLM (code_analysis, code_generation, code_review, code_fix)

### 📁 Директория `docs/`

Документация проекта.

- **INSTALLATION.md**: Детальная инструкция по установке (Docker, локально)
- **ARCHITECTURE.md**: Подробное описание архитектуры, компонентов, потоков данных
- **EXAMPLES.md**: Реальные примеры использования с Issue и PR

### 📁 Директория `scripts/`

Вспомогательные скрипты.

- **setup.sh**: Автоматическая установка (venv, зависимости, .env)

### 🐳 Docker файлы

- **Dockerfile**: Multi-stage образ с Python 3.11
- **docker-compose.yml**: Сервисы code-agent и code-agent-server
- **.dockerignore**: Исключения для Docker сборки

### ⚙️ Конфигурационные файлы

- **pyproject.toml**: Современная конфигурация проекта (PEP 518)
  - Метаданные проекта
  - Зависимости
  - Настройки black, ruff, mypy, pytest
  
- **requirements.txt**: Основные зависимости для продакшена
- **requirements-dev.txt**: Дополнительные dev зависимости

### 📝 Документация

- **README.md**: Главный файл с описанием, quick start, примерами
- **CONTRIBUTING.md**: Руководство для контрибьюторов
- **LICENSE**: MIT лицензия
- **CHANGELOG.md**: История версий и изменений

## Точки входа

### CLI (Командная строка)
```bash
code-agent process-issue 123
code-agent review-pr 456
code-agent fix-pr 456 --feedback "..."
code-agent generate-summary 456
```

### GitHub Actions
- Автоматический запуск при создании Issue
- Автоматический запуск при создании/обновлении PR

### Docker
```bash
docker-compose run code-agent process-issue 123
```

### Python API
```python
from code_agent.agents.code_agent import CodeAgent

agent = CodeAgent()
result = agent.process_issue(123)
```

## Зависимости

### Основные (Production)
- `openai` - OpenAI API
- `pygithub` - GitHub API
- `gitpython` - Git операции
- `typer` - CLI фреймворк
- `rich` - Красивый CLI вывод
- `pydantic` - Валидация и настройки
- `httpx` - HTTP клиент для API
- `pyyaml` - YAML парсинг

### Разработка (Development)
- `pytest` - Тестирование
- `pytest-cov` - Покрытие кода
- `pytest-asyncio` - Async тесты
- `mypy` - Статическая проверка типов
- `ruff` - Быстрый линтер
- `black` - Автоформатирование
- `types-*` - Type stubs

## Конфигурация

### Переменные окружения (.env)
```env
GITHUB_TOKEN=...
GITHUB_REPO=owner/repo
OPENAI_API_KEY=...
LLM_PROVIDER=openai
MAX_ITERATIONS=5
LOG_LEVEL=INFO
```

### Промпты (config/prompts.yaml)
Настраиваемые промпты для различных операций LLM.

## Workflow процессов

### 1. Обработка Issue → PR
```
Issue Created
  ↓ (GitHub Actions)
Code Agent
  ↓ (LLM)
Code Generated
  ↓ (Git)
PR Created
```

### 2. Ревью PR
```
PR Created/Updated
  ↓ (GitHub Actions)
CI Checks + AI Review
  ↓ (Analysis)
Review Posted
  ↓ (If needed)
Auto-fix Applied
```

### 3. Итеративное улучшение
```
Review Feedback
  ↓ (Code Agent)
Code Fixed
  ↓ (Push)
PR Updated
  ↓ (Repeat until approved or max iterations)
```

## Расширение системы

### Добавление нового LLM провайдера
1. Создать класс в `core/llm.py`, наследующий `LLMProvider`
2. Реализовать метод `generate()`
3. Добавить в `get_llm_provider()`
4. Добавить конфигурацию в `config.py`

### Добавление новой команды CLI
1. Добавить функцию с декоратором `@app.command()` в `cli.py`
2. Реализовать логику
3. Добавить тесты
4. Обновить документацию

### Добавление нового workflow
1. Создать `.github/workflows/new-workflow.yml`
2. Определить триггеры и jobs
3. Добавить необходимые secrets
4. Протестировать

## Качество кода

### Линтинг
```bash
ruff check .              # Проверка
ruff check --fix .        # Автоисправление
```

### Форматирование
```bash
black --check .           # Проверка
black .                   # Форматирование
```

### Типы
```bash
mypy code_agent           # Проверка типов
```

### Тесты
```bash
pytest                    # Все тесты
pytest --cov=code_agent   # С покрытием
```

## Деплой

### Локально
```bash
./scripts/setup.sh
source venv/bin/activate
code-agent --help
```

### Docker
```bash
docker-compose build
docker-compose up -d
```

### Cloud (будущее)
- Cloud.ru
- Yandex Cloud
- AWS/GCP/Azure

---

**Версия**: 1.0.0  
**Последнее обновление**: 2024-01-29  
**Статус**: Production Ready

