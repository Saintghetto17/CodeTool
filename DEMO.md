# Демонстрация Code Agent

Этот документ содержит пошаговую инструкцию для демонстрации возможностей Code Agent.

## Подготовка

### 1. Создайте тестовый репозиторий на GitHub

```bash
# Создайте новый репозиторий через GitHub UI или CLI
gh repo create code-agent-demo --public
cd code-agent-demo
git init
```

### 2. Добавьте базовый код

Создайте простой Python проект для демонстрации:

```bash
# Создайте структуру
mkdir src tests
touch src/__init__.py tests/__init__.py
```

**src/calculator.py**:
```python
def add(a: int, b: int) -> int:
    """Add two numbers."""
    return a + b

def subtract(a: int, b: int) -> int:
    """Subtract b from a."""
    return a - b
```

**tests/test_calculator.py**:
```python
from src.calculator import add, subtract

def test_add():
    assert add(2, 3) == 5

def test_subtract():
    assert subtract(5, 3) == 2
```

### 3. Настройте Code Agent

```bash
# Клонируйте Code Agent
git clone https://github.com/your-username/code-agent.git
cd code-agent

# Настройте .env
cp env.example .env
nano .env  # Добавьте GITHUB_TOKEN, OPENAI_API_KEY, GITHUB_REPO
```

### 4. Скопируйте workflows в тестовый репозиторий

```bash
# Из директории code-agent
cp -r .github ../code-agent-demo/
cd ../code-agent-demo
git add .github
git commit -m "Add Code Agent workflows"
git push origin main
```

## Демо-сценарии

### Сценарий 1: Простое добавление функции

**Шаг 1**: Создайте Issue #1
```markdown
Title: Add multiply function

Description:
Add a multiply(a, b) function to src/calculator.py

Requirements:
- Takes two integers
- Returns their product
- Add type hints
- Add docstring
- Add test in tests/test_calculator.py
```

**Шаг 2**: Добавьте метку `code-agent` (если настроено) или запустите вручную:
```bash
cd /path/to/code-agent
code-agent process-issue 1 --repo-path /path/to/code-agent-demo
```

**Ожидаемый результат**:
- ✅ Создана ветка `agent/issue-1`
- ✅ Добавлена функция `multiply` в `calculator.py`
- ✅ Добавлен тест в `test_calculator.py`
- ✅ Создан PR

**Шаг 3**: Проверьте PR
- GitHub Actions автоматически запустит CI и review
- AI Reviewer проанализирует код и даст фидбек

### Сценарий 2: Исправление бага

**Шаг 1**: Создайте баг в коде
```python
# src/calculator.py
def divide(a: int, b: int) -> float:
    """Divide a by b."""
    return a / b  # Bug: no check for b == 0
```

**Шаг 2**: Создайте Issue #2
```markdown
Title: Fix division by zero bug

Description:
The divide() function crashes when b is 0.

Steps to reproduce:
1. Call divide(10, 0)
2. Get ZeroDivisionError

Expected:
- Raise ValueError with clear message
- Add test for edge case
```

**Шаг 3**: Запустите Code Agent
```bash
code-agent process-issue 2 --repo-path /path/to/code-agent-demo
```

**Ожидаемый результат**:
- ✅ Добавлена проверка на `b == 0`
- ✅ Добавлен тест для edge case
- ✅ PR создан с исправлением

### Сценарий 3: Итеративное улучшение

**Шаг 1**: Создайте Issue #3 с неполными требованиями
```markdown
Title: Add statistics module

Description:
Add average function
```

**Шаг 2**: Code Agent создаст базовую реализацию

**Шаг 3**: AI Reviewer запросит улучшения:
```markdown
Changes Requested:
1. Add docstring
2. Handle empty list case
3. Add type hints
4. Add tests
```

**Шаг 4**: Code Agent автоматически исправит и обновит PR

**Ожидаемый результат**:
- ✅ Несколько итераций улучшений
- ✅ Финальный код соответствует всем стандартам
- ✅ PR одобрен после 2-3 итераций

### Сценарий 4: Комплексная фича

**Шаг 1**: Создайте Issue #4
```markdown
Title: Add REST API endpoint for calculator

Description:
Create FastAPI endpoint for calculator operations.

Requirements:
- POST /api/calculate
- Accept JSON: {"operation": "add|subtract|multiply|divide", "a": int, "b": int}
- Return JSON: {"result": float}
- Handle errors gracefully
- Add OpenAPI documentation
- Write integration tests
```

**Шаг 2**: Code Agent создаст:
- Новый файл `api.py` с FastAPI app
- Pydantic модели для валидации
- Error handling
- Тесты с TestClient

**Шаг 3**: CI проверит:
- ✅ Код проходит линтинг
- ✅ Типы корректны
- ✅ Все тесты проходят

**Шаг 4**: AI Reviewer одобрит после проверки

## Метрики для демонстрации

Во время демонстрации обратите внимание на:

### ⏱️ Скорость
- Время от Issue до PR: ~2-5 минут
- Время на итерацию исправлений: ~1-2 минуты
- Время на ревью: ~1-2 минуты

### 🎯 Качество
- Проходит все линтеры (ruff, black, mypy)
- Покрытие тестами
- Следование best practices
- Понятные docstrings

### 🔄 Автоматизация
- Автоматическое создание веток
- Автоматическое создание PR
- Автоматический ревью
- Итеративные исправления без участия человека

## Типичные проблемы и решения

### Проблема: PR создан, но пустой
**Решение**: Проверьте логи, возможно LLM не смог определить какие файлы изменить

### Проблема: Бесконечный цикл исправлений
**Решение**: Проверьте `MAX_ITERATIONS` в .env, по умолчанию 5

### Проблема: AI Reviewer всегда запрашивает изменения
**Решение**: Уточните промпты в `config/prompts.yaml`

## Команды для мониторинга

### Проверка статуса
```bash
# Проверить Issues
gh issue list --repo code-agent-demo

# Проверить PRs
gh pr list --repo code-agent-demo

# Проверить Actions
gh run list --repo code-agent-demo
```

### Просмотр логов
```bash
# GitHub Actions logs
gh run view <run-id> --log

# Локальные логи Code Agent
code-agent process-issue 1 --log-level DEBUG
```

## Расширенные сценарии

### Мультиязычная поддержка
Создайте Issue на русском языке - Code Agent поймет и создаст код.

### Рефакторинг
Попросите улучшить существующий код - Code Agent выполнит рефакторинг.

### Документация
Попросите добавить документацию - Code Agent создаст README или docstrings.

## Впечатляющие моменты для демо

1. **Автоматизация полного цикла**: От Issue до mergeable PR без участия человека
2. **Понимание контекста**: Code Agent анализирует всю кодовую базу
3. **Качество кода**: Соответствует всем стандартам проекта
4. **Итеративность**: Сам исправляет ошибки на основе фидбека
5. **CI/CD интеграция**: Учитывает результаты линтеров и тестов

## Заключение

Code Agent демонстрирует:
- ✅ Полную автоматизацию SDLC
- ✅ Высокое качество генерируемого кода
- ✅ Интеграцию с GitHub экосистемой
- ✅ Масштабируемость и расширяемость
- ✅ Production-ready решение

## Следующие шаги

После успешной демонстрации:
1. Разверните в production репозитории
2. Настройте для специфики вашего проекта
3. Расширьте промпты под ваш стиль кода
4. Добавьте custom правила ревью
5. Интегрируйте с вашим workflow

---

**Время на демо**: ~15-20 минут  
**Уровень сложности**: Средний  
**Впечатляющий эффект**: 🔥🔥🔥

